# 功能测试用例表

> **项目：** 菠萝蓝牙耳机售后 AI 聊天机器人
> **负责人：** 成员 B
> **任务：** 5.4 编写测试用例

---

## 测试环境
- **设备：** iPhone 14 / 微信开发者工具
- **微信版本：** 8.0+
- **测试日期：** 2026-05-17
- **后端环境：** FastAPI 本地开发服务器
- **AI 模型：** DeepSeek API

---

## 正常流程测试用例（10 个）

| 用例ID | 模块 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |
|--------|------|---------|---------|---------|---------|
| TC-001 | 聊天 | 发送文字消息并收到 AI 回复 | 已打开聊天页，网络正常 | 1. 输入文字「AirPods怎么连接」 2. 点击发送按钮 | 消息列表显示用户消息（右蓝），3-8秒后显示 AI 回复（左灰） |
| TC-002 | 聊天 | 点击快捷问题自动发送 | 在聊天页 | 1. 点击快捷问题栏中的「AirPods怎么连接？」 | 自动填入并发送，收到 AI 回复 |
| TC-003 | 聊天 | 输入框为空时发送按钮不可用 | 在聊天页，输入框为空 | 1. 观察发送按钮状态 | 发送按钮灰色不可点击 |
| TC-004 | 聊天 | 输入框有内容时发送按钮变蓝 | 在聊天页 | 1. 输入任意文字 2. 观察发送按钮 | 发送按钮变为蓝色可点击状态 |
| TC-005 | 聊天 | 发送后输入框清空 | 输入框有内容 | 1. 输入文字后点击发送 | 消息发出后输入框自动清空 |
| TC-006 | 聊天 | loading 状态显示 | 网络正常 | 1. 发送一条消息 | 发送后消息列表底部出现 typing 动画（三个灰色点跳动），收到回复后消失 |
| TC-007 | 聊天 | 消息列表自动滚到底部 | 消息超过一屏 | 1. 发送多条消息使列表超过一屏 2. 继续发送新消息 | 新消息出现后自动滚动到列表底部 |
| TC-008 | 管理员 | 新增知识条目 | 在管理员后台页 | 1. 点击「+ 新增」 2. 填写分类、问题、回答、关键词 3. 点保存 | 弹窗关闭，显示「新增成功」，列表刷新 |
| TC-009 | 管理员 | 编辑知识条目 | 列表有数据 | 1. 点击某条的「编辑」 2. 修改问题文字 3. 点保存 | 显示「修改成功」，列表中该条问题已更新 |
| TC-010 | 管理员 | 删除知识条目 | 列表有数据 | 1. 点击某条的「删除」 2. 确认弹窗点「删除」 | 显示「已删除」，列表该条消失 |

---

## 异常流程测试用例（5 个）

| 用例ID | 模块 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |
|--------|------|---------|---------|---------|---------|
| TC-011 | 聊天 | 网络断开时发送消息 | 关闭手机网络 | 1. 输入文字 2. 点击发送 | 消息列表显示错误提示「网络连接失败，请检查网络后重试」 |
| TC-012 | 聊天 | AI 接口返回错误/超时 | 模拟后端返回 500 错误或超时 | 1. 发送消息 | 15秒后显示「AI响应超时，请稍后重试」，可点击重试 |
| TC-013 | 聊天 | 输入特殊字符和 Emoji | 在聊天页 | 1. 输入「😭耳机坏了💔!!!!@#$%」 2. 发送 | 消息正常发送和显示，不出现乱码 |
| TC-014 | 聊天 | 发送空消息（仅空格） | 在聊天页 | 1. 输入多个空格 2. 点击发送 | 不发送消息，保持在当前页面 |
| TC-015 | 管理员 | 保存空表单 | 在新增弹窗，表单全空 | 1. 点击保存 | 提示「请输入问题」，不关闭弹窗 |

---

## 边界条件测试用例（5 个）

| 用例ID | 模块 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |
|--------|------|---------|---------|---------|---------|
| TC-016 | 聊天 | 连续快速发送多条消息 | 网络正常 | 1. 连续快速点击发送 5 条消息 | 消息按顺序发送和显示，loading 期间发送按钮禁用，不会重复发送 |
| TC-017 | 聊天 | 发送长文本消息（200字以上） | 在聊天页 | 1. 输入 200+ 字文本 2. 发送 | 消息气泡正常显示，文字自动换行，气泡宽度不超过72%屏幕 |
| TC-018 | 聊天 | 非耳机相关问题（边界意识） | 网络正常 | 1. 发送「今天天气怎么样？」 2. 发送「帮我写个Python脚本」 | AI 回复应礼貌引导回耳机相关问题，不直接回答非耳机问题 |
| TC-019 | 聊天 | 多次重试失败消息 | 网络断开 | 1. 发送消息失败 2. 点击错误提示重试 3. 重复3次 | 每次重试都重新发送请求，不重复增加错误消息 |
| TC-020 | 管理员 | 分类筛选无结果 | 选择某个分类，该分类无数据 | 1. 点击空分类 | 列表显示「暂无匹配结果」 |

---

## 测试统计

| 指标 | 数值 |
|------|------|
| 总计用例 | 20 |
| 正常流程 | 10 |
| 异常流程 | 5 |
| 边界条件 | 5 |
| 通过 | 0（待测） |
| 失败 | 0（待测） |
| 阻塞 | 0（待测） |

---

## 测试执行备注

- TC-012 需要模拟后端异常（可在测试时临时关闭后端服务）
- TC-018 边界意识依赖系统提示词质量，需在 AI 模型集成完成后测试
- 建议按优先级执行：先正常流程 → 再异常流程 → 最后边界条件
